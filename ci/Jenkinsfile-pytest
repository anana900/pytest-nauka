node
{
	echo "Welcome!"

	stage('raport') 
	{
		try
		{	
				sh "pwd"
    			publishHTML([
    				 allowMissing: false,
    		         alwaysLinkToLastBuild: true,
    		         keepAll: true,
    		         reportDir: 'pytest-przyklad/',
    		         reportFiles: 'wyniki.html',
    		         reportName: 'Pytest-Raport-Testów'
    		         ])
		}
		catch(err)
		{
			echo err
		}
	}
}